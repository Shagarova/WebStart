Формы
1)Атрибут action=""
если поставить /, то отправит на главную страницу
2)метод method GET POST
get - отправляет информацию в виде строки

?key=value&keyN=valueN
post - отправляет данные пакетно, информацию делит(указывается в любом случае, если отправляются файлы)
3)enctype="multipart/form-data" - необходим для того чтобы отправить файлы( в других случаях не обязателен).

в name - пишется имя, оно может повторяться
например инпуты с одним именем и типом радио могут иметь разные значения.
если name у них одинаковый, то чекается только один

у чекбркса кликаются все и передаются вместе с именем, если имя одно, то передается только первый чекнутый, если задать в имени после одного имени [], то будут переданы все и сформирован массив значений.
